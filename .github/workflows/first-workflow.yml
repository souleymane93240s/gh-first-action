name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: greeting
        run: echo "hello world!"
      - name: print goodbye
        run: echo "done - bye!"
